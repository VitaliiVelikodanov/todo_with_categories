services:
  backend:
    build: ./backend
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: file:/data/data.sqlite
      PORT: 3000
    volumes:
      - sqlite_data:/data

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: /backend
    ports:
      - '8080:80'
    depends_on:
      - backend

volumes:
  sqlite_data:
